class Magazine{ 

    constructor(title, editor, release, cadence, type, price, copies, discount) 

    magazineToString(){ 

    } 

    getPublicPrice(){ 
// price di partenza + tasse 20% - eventuale discount in % + 30%;
    }
}