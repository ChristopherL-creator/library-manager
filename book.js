class Book{ 

    constructor(title, author, editor, type, price, copies, pages, yop, discount) 

    bookToString(){ 

    } 

    getPublicPrice(){ 
// price di partenza + tasse 10% - eventuale discount in % + 30%;
    }
}